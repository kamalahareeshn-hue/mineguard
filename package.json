{
  "name": "mineguard-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix mineguard",
    "build": "npm run build --prefix mineguard",
    "start": "npm run start --prefix mineguard",
    "lint": "npm run lint --prefix mineguard"
  }
}
